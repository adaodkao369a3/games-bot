{
  "name": "bob-kun-discord-bot",
  "version": "1.0.0",
  "description": "Bob Kun - A cute, chaotic Minion-inspired gaming Discord bot",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx --watch src/index.ts",
    "build": "tsc && npm run copy-assets",
    "start": "node dist/index.js",
    "copy-assets": "node -e \"const fs = require('fs'); const path = require('path'); const src = path.join(process.cwd(), 'assets'); const dest = path.join(process.cwd(), 'dist', 'assets'); if (!fs.existsSync(dest)) fs.mkdirSync(dest, { recursive: true }); const copyDir = (src, dest) => { fs.readdirSync(src).forEach(file => { const srcPath = path.join(src, file); const destPath = path.join(dest, file); if (fs.lstatSync(srcPath).isDirectory()) { if (!fs.existsSync(destPath)) fs.mkdirSync(destPath, { recursive: true }); copyDir(srcPath, destPath); } else { fs.copyFileSync(srcPath, destPath); } }); }; copyDir(src, dest); console.log('Assets copied to dist/assets');\""
  },
  "keywords": [
    "discord",
    "bot",
    "gaming",
    "minion"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@napi-rs/canvas": "^1.0.6",
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5",
    "emoji-regex": "^10.6.0",
    "gifenc": "^1.0.3",
    "pg": "^8.11.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "@types/pg": "^8.11.0",
    "tsx": "^4.23.12",
    "typescript": "^5.3.3"
  }
}
